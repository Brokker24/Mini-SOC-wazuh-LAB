	<!-- base: sysmon process create (event ID 1) -->
	<rule id="100300" level="5">
	  <if_sid>61603</if_sid>
	  <description>base: sysmon process create</description>
	</rule>

	<!-- suspicious poershell: encodedcommand -->
	<rule id="100301" level="10">
	  <if_matched_sid>100300</if_matched_sid>
	  <match>EncodedCommand</match>
	  <description>Suspicious Powershell Execution (Encoded Command)</description>
	</rule>

	<!-- suspicious Powershell:IEX -->
	<rule id="100302" level="10">
	  <if_matched_sid>100300</if_matched_sid>
	  <match>IEX</match>
	  <description>Suspicious powershell execution (IEX)</description>
	</rule>

	<!-- suspicious powershell: downloadString / Invoke-Webrequest -->
	<rule id="100303" level="10">
	  <if_matched_sid>100300</if_matched_sid>
	  <match>DownloadString</match>
	  <description>suspicious powershell execution (DownloadString / Invoke-Webrequest)</description>
	</rule>
